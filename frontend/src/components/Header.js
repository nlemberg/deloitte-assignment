import logo from "../assets/deloitte-logo.svg";

const Header = () => {
  return (
    <header>
      <img className="logo" src={logo} alt="Deloitte logo" />
    </header>
  );
};

export default Header;
